[build]
  # Node.js build command for TypeScript compilation
  command = "npm ci && npm run build"
  # Output directory where compiled files go
  publish = "dist"

[build.environment]
  # Node.js version to use
  NODE_VERSION = "18"
